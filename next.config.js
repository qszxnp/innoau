/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Outputs a static HTML/CSS/JS export
  distDir: 'out', // Changes the build output directory to 'out'
};

module.exports = nextConfig; 